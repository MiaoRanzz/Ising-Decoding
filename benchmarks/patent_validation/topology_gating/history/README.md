# 历史实验说明

2026 年早期实验把候选连通分量直接当作待提交修正集合，并比较逐位置提交、密度门控
和整簇效用门控。该设计能作为问题发现和基线线索，但不满足 v2 的“簇内合法组合、
逐组合试探、每簇至多选择一个组合、状态更新后重算”定义。

因此旧报告中的正向结论只标记为 **v1 历史结果**，不得作为 v2 的完整验证证据。
旧原始 JSON 和检查点不迁入 Git；其临时副本位于本次整理产生的 `/tmp` 备份中。
