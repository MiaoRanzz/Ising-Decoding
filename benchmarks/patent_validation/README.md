# 专利验证工作区

本目录只保存可审查的验证协议、权利点映射和轻量证据索引。实现位于
`code/`，冻结配置位于 `conf/experiments/patent/`，运行生成物写入已被
Git 忽略的 `outputs/patent_validation/`。专利正文及附图继续单独保存在
被 Git 忽略的 `patent/`，不得把未公开专利内容推送到未经确认的远端。

当前优先项目是 `topology_gating/`，目标为拓扑残差效用门控交底书 v2。
旧实验的摘要只作为历史线索，不能作为 v2 证据。
